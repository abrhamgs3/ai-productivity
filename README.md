# AI and Productivity

Panel econometrics research pipeline studying the relationship between AI adoption
and Total Factor Productivity (TFP) across 193 countries, 2010–2024.

---

## Quick start

### 1. Install

```bash
# clone the repository
git clone https://github.com/<your-username>/ai-productivity.git
cd ai-productivity

# create a virtual environment and install the package
uv venv
uv pip install -e ".[dev]"
```

### 2. Add your data

Place the following files in the indicated directories (see `scripts/` for
download helpers):

```
data/raw/wdi.csv          World Development Indicators
data/raw/pwt.csv          Penn World Tables 10.x
data/raw/ai_proxy.csv     AI adoption proxy (e.g. ChatGPT usage, patent counts)
data/processed/panel_clean.csv   ← generated by scripts/02_clean_data.py
```

### 3. Check your environment

```bash
ai-productivity doctor
```

All checks should show ✔ before running the pipeline.

### 4. Run the pipeline

```bash
ai-productivity run
```

This executes in order:
1. **Validate** — checks required columns, duplicates, missing values
2. **Load** — drops aggregate/non-sovereign entities
3. **Econometrics** — 12 models across robustness, sensitivity, and falsification suites
4. **Tables** — CSV + LaTeX summaries in `tables/`
5. **Figures** — PDF + PNG publication figures in `figures/`
6. **Narratives** — LaTeX results sections in `paper/sections/`

---

## CLI reference

```
Usage: ai-productivity [OPTIONS] COMMAND [ARGS]...

  AI and Productivity — panel econometrics research pipeline.

Options:
  -V, --version  Show version and exit.
  --help         Show this message and exit.

Commands:
  doctor  Verify that the environment is ready to run the pipeline.
  run     Run the full analysis pipeline.
```

### `ai-productivity run` options

| Option | Default | Description |
|---|---|---|
| `--data-path` | `data/processed/panel_clean.csv` | Input panel CSV |
| `--tables-dir` | `tables/` | Output directory for tables |
| `--figures-dir` | `figures/` | Output directory for figures |
| `--paper-dir` | `paper/sections/` | Output directory for LaTeX narratives |
| `--verbose / -v` | off | Enable DEBUG-level logging |

---

## Project layout

```
ai-productivity/
├── src/
│   └── ai_productivity/        # installable package
│       ├── __init__.py         # version, public exceptions
│       ├── cli.py              # Typer CLI (entry point)
│       ├── pipeline.py         # orchestrates all steps
│       ├── exceptions.py       # domain exception hierarchy
│       ├── logging.py          # structured logging helpers
│       ├── data/
│       │   ├── loaders.py      # load_panel(), drop_aggregate_entities()
│       │   ├── validators.py   # validate_data(), report_has_blockers()
│       │   └── cleaning.py     # sample_selection_summary()
│       ├── econometrics/
│       │   └── panel.py        # PanelOLS models (linearmodels)
│       ├── features/
│       │   └── engineering.py  # log transforms, sub-indices
│       ├── visualization/
│       │   ├── style.py        # publication style (ColorBrewer, 300 DPI)
│       │   └── figures.py      # scatter, trend, forest, missingness plots
│       └── reporting/
│           └── narrative.py    # LaTeX narrative generation
├── agents/                     # backward-compat shims (re-export from src/)
├── tests/                      # pytest test suite (25 tests)
├── data/
│   ├── raw/                    # source CSVs (not tracked by git)
│   └── processed/              # cleaned panel (not tracked by git)
├── tables/                     # generated by pipeline (not tracked by git)
├── figures/                    # generated by pipeline (not tracked by git)
├── paper/
│   └── sections/               # LaTeX narratives auto-written by pipeline
├── scripts/                    # data download and cleaning scripts
├── .github/workflows/ci.yml    # GitHub Actions CI
├── pyproject.toml              # package metadata, dependencies, CLI entry point
└── CHANGELOG.md
```

---

## Development

```bash
# run tests
pytest tests/ -v

# lint and format
ruff check src/ tests/
ruff format src/ tests/

# build wheel
python -m hatchling build
```

CI runs automatically on every push to `main` and `dev`
(Python 3.11 and 3.12 matrix).

---

## Paper

> **"AI Adoption and Total Factor Productivity: Panel Evidence from 193 Countries"**
>
> Examines whether AI adoption — proxied by internet penetration, mobile
> subscriptions, secure server density, and AI-related patent filings —
> is associated with TFP gains across a 15-year cross-national panel.
> Identification relies on entity and time fixed effects with clustered
> standard errors (Driscoll-Kraay) to account for cross-sectional dependence.

---

## License

MIT © Ab
